services:
  video-studio:
    volumes:
      - ./app:/app/app
      - ./scripts:/app/scripts
      - ./data:/app/data
      - ./models:/models
    command: uvicorn app.main:app --host 0.0.0.0 --port 7860 --reload
